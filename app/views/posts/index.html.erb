<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 p-4">
  <% if @posts.any? %>
    <%= render @posts %>
  <% else %>
    <div class="col-span-full mb-3 text-gray-600"><%= t('posts.none') %></div>
  <% end %>
</div>