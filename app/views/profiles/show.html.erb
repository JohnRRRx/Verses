<div class="flex-grow container mx-auto px-4 py-6 flex items-center pt-20 justify-center">
  <div class="w-auto md:w-[450px] max-w-full border-2 border-white rounded-2xl p-8 shadow-xl">
    <div class="flex justify-center items-center mb-5">
      <h1 class="text-2xl font-bold text-center border-gray-300 text-gray-700"></h1>
    </div>
    <div class="flex justify-center mb-4">
          <%= image_tag(current_user.icon_url.to_s, class: 'rounded-full w-48 h-48 object-cover') if current_user.icon_url.present? %>
            </div><table class="w-full max-w-full">
      <tbody>
          <tr class="border-b text-gray-700">
          <th class="py-2 px-4 text-left text-nowrap max-w-full text-auto-size"><%= User.human_attribute_name(:name) %></th>
          <td class="py-2 px-4 max-w-full text-auto-size"><%= current_user.name %></td>
          </tr>

        <tr class="border-b text-gray-700">
          <th class="py-2 px-4 text-left text-nowrap max-w-full text-auto-size"><%= User.human_attribute_name(:email) %></th>
          <td class="py-2 px-4 max-w-full text-auto-size"><%= current_user.email %></td>
        </tr>
      </tbody>
    </table>
    <div class= "flex justify-center mt-4">
    <%= link_to t('posts.edit'), edit_profile_path, class: "text-center w-full py-2 px-4 font-semibold border border-transparent rounded-md shadow-sm text-sm text-white bg-blue-400 hover:bg-blue-500 focus:outline-none" %>
    </div>
  </div>
</div>