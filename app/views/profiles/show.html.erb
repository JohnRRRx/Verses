<div class="flex-grow container mx-auto px-4 py-6 flex items-center justify-center">
  <div class="w-[450px] border-2 border-white rounded-2xl p-8">
    <div class="flex justify-center items-center mb-5">
      <h1 class="text-2xl font-bold text-center"><%= t('header.profile') %></h1>
    </div>
    <div class="flex justify-center mb-4">
          <%= image_tag(current_user.icon_url.to_s, class: 'rounded-full w-48 h-48') if current_user.icon_url.present? %>
            </div><table class="w-full">
      <tbody>
          <tr class="border-b">
          <th class="py-2 px-4 text-left"><%= User.human_attribute_name(:name) %></th>
          <td class="py-2 px-4"><%= current_user.name %></td>
          </tr>

        <tr class="border-b">
          <th class="py-2 px-4 text-left"><%= User.human_attribute_name(:email) %></th>
          <td class="py-2 px-4"><%= current_user.email %></td>
        </tr>
      </tbody>
    </table>
    <div class= "flex justify-center mt-4">
    <%= link_to t('posts.edit'), edit_profile_path, class: 'bg-violet-600 hover:bg-violet-700 text-white font-bold py-2 px-4 rounded' %>
    </div>
  </div>
</div>